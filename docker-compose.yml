version: "3.1"

services:
  openresty:
    build: .
    command: -c /etc/nginx/compose.conf
    environment:
      SESSION_COOKIE_NAME: custom-analytics
    networks:
      - qliktivecustomanalytics_default
    ports:
      - "80:80"

networks:
  qliktivecustomanalytics_default:
    external: true
